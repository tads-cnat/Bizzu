export interface IBeePerfilResumoCard {
  // Nome da pessoa ou perfil que será exibido no card
  nome: string

  // Breve descrição ou resumo do perfil
  descricao: string

  // URL do perfil do LinkedIn (opcional)
  linkedinUrl?: string

  // URL da imagem de banner que será exibida no card (opcional)
  bannerUrl?: string
}
