export interface IBeeDropPost{
    // Nome que será usado para exibir no dropPost tipo "excluir repositório ou postagem"
    excluir?: string
    // Nome que será usado para exibir no dropPost tipo "editar repositório ou postagem"
    editar?: string
}