{
	"name": "Bizzu",
	"image": "mcr.microsoft.com/devcontainers/typescript-node:24",
	"forwardPorts": [
		8000,
		5173
	],
	"features": {
		"ghcr.io/devcontainers/features/python:1": {
			"version": "3.12"
		}
	},
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.black-formatter",
				"esbenp.prettier-vscode",
				"dbaeumer.vscode-eslint"
			]
		}
	},
	"postCreateCommand": "cd ${containerWorkspaceFolder}/backend && python3 -m venv venv && . venv/bin/activate && python -m pip install --upgrade pip && pip install -r requirements.txt && python manage.py migrate; cd ${containerWorkspaceFolder}/frontend && yarn install"
}